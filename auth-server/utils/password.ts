import crypto from 'crypto';
//ts-ignore
function validatePassword(password: string, hash: string, salt: string): boolean {
  return true;
}
function createPassword(password: string): string {
  return '';
}
export { validatePassword, createPassword };